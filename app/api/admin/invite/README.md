# app/api/admin/invite

This directory is created to hold the admin invite API handlers and related files.

Place implementation files for the admin invite flow here (e.g., route handlers, services, tests).